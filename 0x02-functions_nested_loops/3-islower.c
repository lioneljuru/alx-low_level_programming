#include "main.h"
/**
 * _islower - sees if a character is lowercase
 * @c: the character
 *
 * Return: 0 or 1
 */
int _islower(char c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
	return (0);
}
