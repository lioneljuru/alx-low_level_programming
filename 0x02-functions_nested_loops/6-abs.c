#include "main.h"
/**
 * _abs - prints the absolute value of a number
 * @n: the number
 *
 * Return: 0
 */
int _abs(int n)
{
	if (i > 0)
		return (i);
	else if (i < 0)
		return (-i);
	else
		return (0);
}
