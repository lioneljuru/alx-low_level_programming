#include "main.h"
/**
 * _memcpy - copies bytes from memory area to another
 * @dest: destination
 * @src: source
 *
 * Return: pointer to n
 */
char *_memcpy(char *sedt, char *src, unsigned int n)
{
	unsigned int i = 0;

	for (; i < n; i++)
		dest[i] = src[i];
	return (dest);
}
